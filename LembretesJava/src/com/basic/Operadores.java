package com.basic;

public class Operadores {

    //Operadores Aritméticos
    int a = 5, b = 2;    //Variáveis para exemplos.

    //Operações comuns
    int soma = a + b;
    int subtrai = a - b;
    int divide = a / b;
    int multiplica = a * b;
    int resto = a % b;

    //Operadores de incremento/decremento
    int somaAntes = ++a;        //Incrementa a variáel antes de retorna-la.
    int somaDepois = a++;       //Retorna a variável e depois incrementa.
    int subtraiAntes = --a;     //Decrementa a variáel antes de retorna-la.
    int subtraiDepois = a--;    //Retorna a variável e depois decrementa.

    //Operadores de atribuição
    /*
    numero += a;    //Equivale a: numero = numero + a.
    numero -= a;    //Equivale a: numero = numero - a.
    numero *= a;    //Equivale a: numero = numero * a.
    numero /= a;    //Equivale a: numero = numero / a.
    numero %= a;    //Equivale a: numero = numero % a.
     */

    //Operadores relacionais
    /*
    Similaridade:
    a == b; Verifica se a é igual a b.
    a != b; Verifica se a é diferente de b.
    Tamanho:
    a > b; Verifica se a é maior que b.
    a >= b; Verifica se a é maior ou igual a b.
    a > b; Verifica se a é menor que b.
    a >= b; Verifica se a é menor ou igual a b.
    */

    //Operadores Lógicos
    /*
    a && b; Conjunção. Operador AND.
    a || b; Disjunção. Operador OR.
    a ^ b; Disjunção exclusiva. Operador XOR.
    a ! b; Negação ou Inversão. Operador NOT.
     */
}
